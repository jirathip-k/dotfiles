# Copy to .dotter/local.toml (gitignored) and keep only the packages you want
# deployed on this machine, then run `dotter deploy`.
packages = [
    "nvim",
    "zsh",
    "starship",
    "ghostty",
    "git",
    "tmux",
]
